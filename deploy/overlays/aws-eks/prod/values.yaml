backend:
  replicas: 2
frontend:
  replicas: 2
qdrant:
  persistence:
    storageClassName: gp3
kuberay:
  worker:
    replicas: 2
vllm:
  nodeSelector:
    accelerator: nvidia # TODO: align with EKS GPU node labels.
  tolerations:
    - key: "nvidia.com/gpu"
      operator: "Exists"
      effect: "NoSchedule"
