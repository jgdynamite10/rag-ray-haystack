.DS_Store
.venv/
__pycache__/
node_modules/
.terraform/
terraform.tfstate
terraform.tfstate.*
terraform.tfvars

# --- Local-only project context (do not commit) ---
/docs/PROJECT_STATE.local.md
/docs/OPERATIONS.local.md
/docs/CONTEXT.local.md
/docs/ENDPOINTS.local.md

# --- Scripts with environment-specific IPs (use .example as template) ---
/scripts/run_all_benchmarks.sh

# --- Benchmark results with environment-specific data ---
# Raw JSON results contain IPs and account IDs
# Summary data is in docs/BENCHMARK_RESULTS.md
/benchmarks/

# --- Archived dashboards (local only) ---
/grafana/dashboards/Old-dashboards/

# --- Secrets / env ---
.env
.env.*
**/*token*
**/*secret*
**/*kubeconfig*
**/*.kubeconfig

# --- Terraform state (never commit) ---
**/.terraform/*
**/.terraform.lock.hcl.backup
**/terraform.tfstate
**/terraform.tfstate.*
**/crash.log
**/crash.*.log

# --- Kube configs (never commit) ---
/.kube/
/**/*.yaml.kubeconfig
